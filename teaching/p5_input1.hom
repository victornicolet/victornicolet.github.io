join(int sumL |int sumR) returns (sum) locals (int sum) {
	   sum = sumL + sumR;
}

sequential (int* s, int n) returns (sum) locals (int i, int sum) {
	   sum = 0;
	   i = 0;
	   for(i = 0; i < n; i++) {
	   	 sum = sum + s[i];
	   }
}